import 'package:flutter/material.dart';

class MapConstants{
  MapConstants._();

  static const defaultCameraZoom = 12.0;
  static const routeWidth = 3;
  static const routeColor = Colors.red;
  static const defaultMarketSize = Size.square(120);
}